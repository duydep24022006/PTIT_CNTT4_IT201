#include <stdio.h>

int main(int n) {
    for (int i=0; i < n; i++) {//độ phức tạp là O(n);
        printf("hello\n");//độ phức tạp là O(n);
    }
    return 0;
    //độ phức tạp thời gian của là  O(n);
}